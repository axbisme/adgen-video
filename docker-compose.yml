version: '3.8'

services:
  backend:
    build: ./server
    container_name: adgen_backend
    ports:
      - "5000:5000"
    volumes:
      - ./server/videos:/app/videos
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    env_file:
      - .env
